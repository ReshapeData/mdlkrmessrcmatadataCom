# 
# mdlDemoServer::buttonServer(input = input,output = output,session = session,dms_token = dms_token)
# 
# #完成日期组件注册
# mdlDemoServer::dateServer(input = input,output = output,session = session,dms_token = dms_token)


#jhhrv
# mdljhhrvServer::HrvServer(input = input,output = output,session = session,dms_token = dms_token)

# krmes
mdlkrmessrcmatadataServer::krmesstocksrcmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcmatadataServer::krmesroutesrcmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmessrcmatadataServer::krmesproductionwarehousinsrcmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmessrcmatadataServer::krmesproductionreturnsrcmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmessrcmatadataServer::krmesproductionrequisitionsrcmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmessrcmatadataServer::krmesproductionordersrcmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmessrcmatadataServer::krmesproductionfeedsrcmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmessrcmatadataServer::krmesprocesstransfersrcmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmessrcmatadataServer::krmesprocessreportsrcmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmessrcmatadataServer::krmesprocessplanningsrcmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcmatadataServer::krmesprocesssrcmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcmatadataServer::krmesmaterialsrcmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcmatadataServer::krmesflexvaluessrcmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcmatadataServer::krmesempsrcmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcmatadataServer::krmesdeptsrcmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcmatadataServer::krmesbomsrcmatadataServer(input = input,output = output,session = session,dms_token = dms_token)



  

