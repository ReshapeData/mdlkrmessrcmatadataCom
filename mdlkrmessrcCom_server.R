# 
# mdlDemoServer::buttonServer(input = input,output = output,session = session,dms_token = dms_token)
# 
# #完成日期组件注册
# mdlDemoServer::dateServer(input = input,output = output,session = session,dms_token = dms_token)


#jhhrv
# mdljhhrvServer::HrvServer(input = input,output = output,session = session,dms_token = dms_token)

# krmes
mdlkrmessrcServer::krmesstocksrcServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcServer::krmesroutesrcServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcServer::krmesproductionwarehousinsrcServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcServer::krmesproductionreturnsrcServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcServer::krmesproductionrequisitionsrcServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcServer::krmesproductionordersrcServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcServer::krmesproductionfeedsrcServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcServer::krmesprocesstransfersrcServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcServer::krmesprocessreportsrcServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcServer::krmesprocessplanningsrcServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcServer::krmesprocesssrcServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcServer::krmesmaterialsrcServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcServer::krmesflexvaluessrcServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcServer::krmesempsrcServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcServer::krmesdeptsrcServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcServer::krmesbomsrcServer(input = input,output = output,session = session,dms_token = dms_token)



  

